class Player {
  constructor(map) {
    this.map = map;
    this.position = {
      x: 0,
      y: 0,
    };
  }

  move(newPosition) {
    // if (this.map.canMove(this.position, newPosition));
  }
}

export default class Player;
